# Shift+Space 播放下一项设计

## 目标

在手动逐项分组中，按 `Shift+Space` 播放组内下一项。普通空格继续使用 IINA 原生的播放/暂停行为。

## 设计

通过 IINA 输入监听接收 `Shift+SPACE`，调用现有的下一项决策。插件菜单保留可点击的“播放下一项”，但不绑定快捷键，从而避开 IINA 菜单快捷键限制和系统快捷键冲突。

## 验证

源代码回归测试必须确认输入监听绑定为 `Shift+SPACE`、不存在裸 `SPACE` 菜单绑定，并保留现有全部测试。实机验证必须看到按 `Shift+Space` 后窗口标题切换到第二个视频。
